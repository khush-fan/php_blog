{% extends 'base.twig'%}
{% block body %}
    <div class="container">
        <img src="https://dummyimage.com/1200x300/263ccc/fff.png" class="img-fluid" alt="...">
        <h2 class="mt-3 mb-3">{{ post.title|escape }}</h2>
        <div class="container">
            <div class="row">
                <span class = "text-muted">Фаниль Хущамов | {{ post.published_date|date("F d, Y") }}</span>
            </div>
        </div>
        <div>
            {{ post.content|raw }}
        </div>
    </div>
    
{% endblock %}